<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="LoginMapper">
	
	<select id="login" parameterType="kr.co.goodle.util.dto.MemberDTO" resultType="kr.co.goodle.util.dto.MemberDTO">
		select m.mem_no, m.mem_name, m.mem_id, m.mem_tel, mem_email, mgr_yn, mem_delyn, mem_post, mem_addr1, mem_addr2, plan_no
		from member m
		where mem_id = #{mem_id} and mem_pwd = #{mem_pwd} 
	</select>
</mapper>
